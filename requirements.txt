# === Core ===
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0     # LLM qua Groq API (miễn phí)
langchain-text-splitters>=0.3.0

# === Embedding & Search ===
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0          # tìm kiếm vector; module tự fallback numpy nếu thiếu
numpy>=1.26.0

# === PDF Processing ===
pymupdf>=1.24.0

# === Utilities ===
python-dotenv>=1.0.0
tqdm>=4.66.0

# === Test ===
pytest>=8.0.0
